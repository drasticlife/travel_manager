# 작업 인계 (HANDOFF)

> 새 세션은 이 파일부터 읽는다. 그다음 `CLAUDE.md`.
> 마지막 갱신: 2026-09-09 (4차)

## 지금 상태 한 줄

수집→DB 코어는 **동작한다.** 장소 52건·일정 12건이 들어 있고 테스트 56건이 통과한다.
Todoist 는 99건, 삭제 0.
**확정안은 초록판이다** — DAY2 해변공원+호텔휴식+라라포트(Moff) / DAY3 마린월드+텐진+텐진저녁.
`maps.html` 은 이제 그 가이드를 그대로 옮긴 인포그래픽 페이지다. 남은 건 서브프로젝트 4개다.

## 여행 정보 (마감이 여기서 나온다)

| | |
| --- | --- |
| 일정 | **2026-09-21(월) ~ 09-24(목)**, 3박 4일 |
| 남은 시간 | 2026-09-07 기준 **약 2주** |
| 숙소 | 호텔 포르자 하카타역 치쿠시구치Ⅱ (1-13-3 Hakataekihigashi) |
| 주의 | 실버위크 5연휴(9/19~23) 한복판. 관광지 혼잡, 일부 시설 운영시간 연장 |

**2주밖에 없다.** 새 기능보다 "지금 여행에 실제로 쓰이는 것"을 먼저 끝낸다.

## 바로 다음에 할 일 (우선순위 순)

1. **주소 미확보 28건 채우기** — `/trip lookup` 반복
   ```bash
   python trip.py pending --limit 20   # 목록 받기
   # 장소마다 WebSearch 한국어 1회 + 영어 1회 → 근거 URL 수집
   python trip.py apply-lookup          # stdin JSON
   ```
   **pending 은 0 이 됐다(2026-09-07, Places API 로 처리).**
   현재 `matched 29 / ambiguous 18 / not_found 4 / pending 0`
   남은 일은 조사가 아니라 **ambiguous 18건 사람 확인**이다 — `maps.html` 에서 본다.

2. ~~**지도 링크 확인용 HTML**~~ — **끝났다(2026-09-07). 2026-09-09 에 가이드 페이지로 바뀌었다.**
   ```bash
   python export.py maps-page      # maps.html 생성 (기본 --out maps.html)
   ```
   이제 사용자가 만든 가이드 인포그래픽(초록판) 모양이다: 일차별 색 카드,
   [주요 일정 | 영업시간 | 이동 동선] 3열. **아이콘을 누르면 팝업**이 열려
   주소·노트·근거 URL·구글지도 링크를 보여준다.
   내 지도 확인 흐름은 그대로 살아 있다 — 팝업 안 '내 지도에 저장함' 을 체크하면
   하단 바가 `python trip.py mark-saved <id...>` 를 만들어 준다.
   페이지는 DB 에 쓰지 않는다. 체크 상태는 브라우저 localStorage 에만 있다.
   `maps.html` 은 생성물이라 `.gitignore` 에 넣었다 — 소스는 `maps_page.py`.
   ⚠️ `maps_page.py` 의 `DAY_META`(날짜·공휴일·감성 문구)는 DB 에 없는 값이라
   코드에 박혀 있다. 여행 날짜가 바뀌면 여기도 고쳐야 한다.

3. ~~**Todoist 푸시**~~ — **끝났다(2026-09-07).** 아래 '완료' 절 참고.
   ~~⚠️ `export.py todoist --push` 를 다시 쓰지 마라~~ — **2026-09-09 에 해소됐다.**
   일정 9건 전부에 `itinerary.todoist_task_id` 를 채워서 중복 가드가 로컬에서 먼저 잡는다.
   `python export.py todoist` 가 `{'would_create': 0, 'skipped': 9}` 를 낸다.
   제목 형식이 다른 문제(`1일차 [오후] …` vs `[오후] …`)는 그대로지만, task_id 가
   있으므로 원격 제목 대조까지 갈 일이 없다.
   `packing` 테이블은 비어 있다 — 짐 목록의 원본은 DB 가 아니라 Todoist 다.

4. 노트의 남은 확인 사항 (사람이 해야 하는 것)
   - `7-Eleven`(place id 7) 상호 특정 — 평점 3.1(11), 목록에 왜 담았는지 사용자만 앎
   - 야마야 다이묘점 주소 미확인
   - Klook/Live Japan 러닝화 5% 쿠폰 바코드 캡처
   - DAY2 아침 마린월드 X(@marine_uminaka) 임시휴관 확인
   - 마린월드·해변공원 사전 예매 여부 결정

## 완료: 확정안을 초록판으로 교체 (2026-09-09, 2차)

사용자가 HTML 을 초록판 모양으로 요청하면서 **일정 자체를 초록판으로 확정**했다.
아래 '가이드 이미지 반영' 절에서 핑크판으로 맞춰 놓은 것을 다시 바꾼 것이다.

| | 핑크판(이전) | 초록판(현재) |
| --- | --- | --- |
| DAY2 | 해변공원 + 마린월드 | 해변공원 + 호텔 휴식 + 라라포트·Moff |
| DAY3 | 호빵맨 + 텐진 | 마린월드 + 텐진 + 텐진 저녁식사 |
| DAY4 | (시각 없음) | 체크아웃 11:00 / 출발 18:00 |

- `라라포트 후쿠오카` 를 place 52 로 추가했다. 주소는 같은 건물인 Moff animal
  cafe(place 6)에서 가져왔고 **좌표는 넣지 않았다** — `pending` 이라 재검증 대상이다.
- 호빵맨(place 13)은 일정에서 빠졌다. note 에 태그를 붙였고 Todoist 태스크는 남겼다.
- Todoist: description 6건 갱신, 마린월드를 2일차→3일차로 이동(`POST /tasks/{id}/move`),
  신규 4건 생성, **삭제 0**. 일정 12건 전부 `todoist_task_id` 가 채워졌다.

## 완료: 가이드 이미지 반영 (2026-09-09)

사용자가 만든 가이드 이미지 3장(일정표 2종 + 하카타역 조식 가이드)을 반영했다.
원문은 `source` id=3 에 전문 보존.

- 일정표가 **두 종류**로 왔다. 사용자가 **핑크판을 확정안**으로 골랐다 —
  DAY2 해변공원+마린월드 / DAY3 호빵맨+텐진. 초록판(DAY2 라라포트+Moff,
  DAY3 마린월드)은 채택 안 됐지만 `source.raw_text` 에 같이 남아 있다.
- 가이드에 없는 세부 쇼핑 일정 9건(JUMP SHOP·알펜·요도바시·로피아·애니메이트·
  만다라케·스텝스포츠·야마야·빅카메라)을 `itinerary` 에서 뺐다. 정보는 버리지 않고
  각 `place.note` 로 옮기고 "가이드 확정안(핑크판) 일정에서 제외" 태그를 붙였다.
  Todoist 쪽 해당 태스크는 **손대지 않았다**(삭제 0 원칙). 목록에 그대로 남아 있다.
- 남은 9건의 `memo` 를 가이드의 영업시간·이동경로로 다시 썼고 같은 내용을 Todoist
  description 에 반영했다(8건 갱신, 1건은 이미 동일).

**첨부가 오염 2건을 잡았다** — 함정 5-a 의 실제 피해자다.

| place | 저장돼 있던 것 | 첨부가 알려준 정확한 지점 |
| --- | --- | --- |
| 30 카페 베로체 | `カフェ・ベローチェ 福岡薬院店` (주오구 야쿠인, 2km+) | 하카타역 치쿠시구치 1-11-1 (도보 3~5분) |
| 33 다이후쿠 우동 | `다이후쿠우동 덴키비루점` (주오구 와타나베도리) | JR 하카타역 B1F 하카타 1번가 (도보 5~7분) |

주소는 첨부 근거로 교체하고 **좌표는 NULL 로 지웠다.** 틀린 좌표를 남기면 나중에
동선 계산이 조용히 망가진다. 지어내지 않았다. `verify_status` 는 `ambiguous` 그대로라
재검증 대상으로 남아 있다.

## 완료: Todoist 재구성 (2026-09-07)

3살 아기(다정이)·배우자·본인 3인 가족 기준으로 재편했다. **삭제 0건**, 기존 태스크는
`parent_id` 만 바꿨고 개명은 1건뿐이다(`여행 전 Packing LIST ✈️` → `📄 서류·예약`).

| 섹션 | 그룹 | 건수 |
| --- | --- | --- |
| 여행 전 | 📄 서류·예약 / 👶 다정이(3살) / 🧳 어른 / 🎒 공용·전자 | 21 / 14 / 26 / 18 |
| 여행 중 | 여행 중 💆‍♀️ / 주의사항 / 1~4일차 헤더(due 9/21~24) | 4 / 7 / 22 |
| 여행 후 | 사진 백업 · eSIM 해지 · 면세 정산 | 3 |

일정 18건은 `itinerary` 를 그대로 옮겼고 이동시간·쇼 시각은 description 에 넣었다.
아기 항목 신규 10건(기저귀, 해열제 시럽, 이착륙 귀 먹먹 대비, 미아방지 팔찌, 유모차 …),
서류 확인 신규 7건(소아 좌석, 유모차 게이트 위탁, 보험 3인, 유아 침대 요청 …).

1회용 스크립트는 스크래치패드에 있고 리포에 남기지 않았다.

## 안 한 것 (서브프로젝트, 각각 별도 spec→plan 사이클)

| # | 내용 | 상태 |
| --- | --- | --- |
| 3 | DB 기반 LLM 정보 제공 (최적동선·맛집추천·쇼핑주의) | 미착수. **좌표가 없어 동선 최적화는 막혀 있다** |
| 4 | GitHub Pages 배포 | 미착수. 리포가 private이라 Pages는 Pro 필요 → public 전환 여부 결정 필요 |
| 5-a | 텔레그램 봇 수집 (폰에서 던지기) | 미착수. `trip.py` 위에 어댑터로 얹으면 됨 |
| — | 사진/스크린샷 OCR 입력 | 범위 밖 (사용자가 제외) |

## 반드시 알아야 할 함정 (전부 실제로 밟았다)

1. **Windows stdin/stdout 이 cp949다.** 파이프로 들어온 UTF-8 한국어가 깨진다
   (`맛집` → `留쏆쭛`). `main()` 의 `sys.stdin.reconfigure(encoding="utf-8")` 를 지우지 마라.
   `io.StringIO` 테스트로는 재현되지 않는다 — subprocess 테스트가 잡는다.
2. **Todoist REST v2 는 폐기됐다(410 Gone).** `/api/v1/` 을 쓰고 `{results, next_cursor}` 를
   `unwrap()` 으로 벗긴다. `project_id` 는 브라우저 URL 의 대시 뒤 문자열과 같다.
3. **웹 검색으로 place_id·좌표는 안 나온다. 주소만 나온다.** 그래서 주소 기반 검색 링크를 쓴다.
   Claude 가 좌표를 쓰면 `apply-lookup` 이 E3 으로 거부한다. 이 가드를 풀지 마라.
4. **Google Takeout 은 '저장됨' 목록을 안 내줬다.** '내 지도' 제품만 잡혀 빈 아카이브가 나왔다.
   목록은 브라우저로 직접 읽었다(가상 스크롤이라 스크롤하며 수집).
5. **Places API 는 무료 한도만 써도 결제 수단 등록이 필수다.** 2026-09-07 에 키를 넣어
   연결했고 `trip.py verify` 가 pending 24건을 처리했다. `.env` 의
   `GOOGLE_MAPS_API_KEY` 를 쓴다.
5-a. **지역 제한 없이 이름만 던지면 체인점이 전국에서 잡힌다.** '카페 베로체 치쿠시구치'
   를 찾다가 **도쿄 니시신주쿠점**의 주소·좌표가 DB 에 써졌다. `places.SEARCH_AREA`
   (후쿠오카 사각형)를 지우지 마라. `judge` 가 ambiguous 로 잡아줘도 주소·좌표는
   이미 써진 뒤다 — 판정이 아니라 **검색 자체를 가둬야** 한다.
6. **일정 정렬은 `day_no, 슬롯순서, seq`.** `seq` 만 쓰면 밤이 오전보다 먼저 나온다.
7. **Todoist 목록 API 는 한 페이지 50건에서 자른다.** `next_cursor` 를 안 따라가면
   중복 검사가 앞의 50건만 보고 공유 프로젝트에 같은 태스크를 또 만든다.
   실제로 115건짜리 프로젝트를 50건으로 읽었다. `export.get_all()` 을 쓸 것.

## 데이터 출처

| 파일 | 내용 |
| --- | --- |
| `data/fukuoka_list.json` | 구글지도 `'26 후쿠오카` 목록 27건 (브라우저로 읽음) |
| `data/lookup_batch1.json` | 위 27건의 주소 조사 결과 + 근거 URL |
| `data/journal_note_import.json` | 볼트 일일노트에서 가져온 장소 24건 + 일정 18건 |
| `data/trip.db` | SQLite 본체 (git 커밋 대상) |

원본 노트: `C:\Vault\Moon Life Planner\03. JOURNALS\2026\2026-M09\2026-W37\후쿠오카 3박 4일 여행 DB.md`

## 문서

- 설계 스펙: `docs/superpowers/specs/2026-09-06-travel-manager-design.md`
- 구현 계획: `docs/superpowers/plans/2026-09-06-collect-to-db-core.md`
- 프로젝트 규약: `CLAUDE.md`
- 사용법: `README.md`
- 확인 페이지 소스: `maps_page.py` (디자인 출처: Newspaper X / Colorlib,
  `G:\내 드라이브
ewspaper-x.1.3.4
ewspaper-x`)

스펙과 코드가 다르면 스펙이 맞다. 코드를 고치거나 스펙을 갱신하고 이유를 남긴다.
